const baseHost = 'http://192.168.0.5'

export const apiMicroservices = {
    userMs: baseHost + ':5000',
    courseMs: baseHost + ':5001',
    blogMs: baseHost + ':5002',
    placementMs: baseHost + ':5003'
}